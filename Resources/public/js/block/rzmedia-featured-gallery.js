var rzmediafeaturedgallery = {
    init:function(id){
        console.log(id);
        if(jQuery(id).length > 0){
            console.log('rommel');
            jQuery(id).imgLiquid();
        }
    }
}
